# L3mcore - Light Easy Mix Of Experts
# Python dependencies

# WSGI production server
flask>=3.0.0
gunicorn>=22.0.0

# Router classifier (embedding + fuzzy fallback)
transformers>=4.30.0
torch                 # CPU build: pip install torch --index-url https://download.pytorch.org/whl/cpu
sentencepiece>=0.1.99
sentence-transformers>=2.2.2
rapidfuzz>=3.0
litellm>=1.0.0

# Local ONNX specialized models
onnxruntime>=1.15.0
numpy>=1.24.0

# GGUF fallback engine
llama-cpp-python>=0.2.0
